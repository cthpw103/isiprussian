var lines = database.split('\n');
var withcheese = '';
for(var i = 0;i < lines.length;i++){
    withcheese = lines[i].split(',')
    for(var e = 0;e < withcheese.length;i++){
        //help
        withcheese[e].split('.')[1]
    }
}
